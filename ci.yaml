name: $(BuildID)

trigger:
  branches:
    include:
    - feature/*
    - fix/*
  paths:
    exclude:
    - '*.yaml'
    - '*.md'
    - 'backends/*'
    - 'images/*'
    - '.github/*'
    - 'docs/*'

stages:
- template: stages/ci.yaml